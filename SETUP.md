# 🚀 Инструкция по установке PARTMART Cards Generator

## 📦 Требования

- Python 3.8+
- pip
- Virtualenv (рекомендуется)

## 🛠 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/vitorpixel-6436/partmart-cards.git
cd partmart-cards
```

### 2. Создание виртуального окружения

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Миграции базы данных

```bash
python manage.py migrate
```

### 5. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### 6. Запуск сервера

```bash
python manage.py runserver
```

Откройте браузер: **http://127.0.0.1:8000**

---

## 🎨 Использование

### Создание карточки

1. Нажмите **"Создать карточку"**
2. Загрузите фото компьютера (drag & drop или выбор)
3. Заполните характеристики:
   - 🖥 Процессор
   - 🎮 Видеокарта
   - 🧠 Оперативка
   - 💾 Накопитель
   - 💰 Цена
   - 🎁 Бонусы
4. Выберите стиль:
   - 🔴 **MSI Gaming** - агрессивный красно-черный
   - 🎮 **Steam Library** - glass morphism
   - 🍎 **Apple Premium** - минимализм
   - 🎵 **Spotify Minimal** - ультра-чистый
5. Нажмите **"Сгенерировать"**
6. Скачайте PNG (1200x1200px)

### Админ-панель

Доступ: **http://127.0.0.1:8000/admin**

Возможности:
- Управление сборками
- Создание пресетов характеристик
- Просмотр сгенерированных карточек

---

## 📚 Структура проекта

```
partmart-cards/
├── manage.py              # Django управление
├── partmart_cards/       # Основные настройки
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── cards/                # Приложение
│   ├── models.py         # Модели (PCBuild, Preset)
│   ├── views.py          # Представления
│   ├── forms.py          # Формы
│   ├── admin.py          # Админка
│   ├── generators/       # Генераторы карточек
│   │   ├── base_generator.py
│   │   ├── msi_style.py
│   │   ├── steam_style.py
│   │   ├── apple_style.py
│   │   └── spotify_style.py
│   └── templates/        # HTML шаблоны
├── static/               # Статика
│   ├── css/
│   │   ├── glass-liquid.css  # Liquid Glass UI
│   │   └── main.css
│   └── js/
│       └── card-generator.js
└── media/                # Загрузки
    ├── uploads/          # Фото ПК
    └── generated/        # Генерация
```

---

## ✨ Особенности

### 🎨 4 Премиум стиля

1. **MSI Gaming** - RGB эффекты, диагональные линии
2. **Steam Library** - Frosted glass, размытый фон
3. **Apple Premium** - Мягкие тени, минимализм
4. **Spotify Minimal** - Чистый дизайн, яркие акценты

### 🔧 Технологии

- **Backend:** Django 5.0 + Python 3.8+
- **Image Processing:** Pillow (PIL Fork)
- **Frontend:** Vanilla JS + Tailwind CSS
- **UI Design:** Apple Liquid Glass Effects
- **Database:** SQLite (dev) / PostgreSQL (prod)

### 💾 Формат экспорта

- **Разрешение:** 1200x1200px
- **Формат:** PNG (high quality)
- **Оптимизация:** Автоматическая
- **Watermark:** Логотип ПАРТМАРТ

---

## 🐛 Решение проблем

### Ошибка: "No module named 'PIL'"

```bash
pip install Pillow
```

### Ошибка: Шрифты не найдены

**Linux:**
```bash
sudo apt-get install fonts-dejavu-core
```

**Windows:** Установите Arial/DejaVu шрифты

### Медленная генерация

- Уменьшите размер исходных фото (<5MB)
- Оптимизируйте фото перед загрузкой

---

## 🚀 Production развертывание

### 1. Настройте переменные окружения

```env
DEBUG=False
SECRET_KEY=your-secret-key
ALLOWED_HOSTS=yourdomain.com
DATABASE_URL=postgresql://...
```

### 2. Соберите статику

```bash
python manage.py collectstatic
```

### 3. Используйте Gunicorn + Nginx

```bash
gunicorn partmart_cards.wsgi:application
```

---

## 📝 Лицензия

MIT License - свободное использование

---

## 👥 Автор

👾 **PARTMART** - Премиум карточки для продажи компьютеров

GitHub: [vitorpixel-6436/partmart-cards](https://github.com/vitorpixel-6436/partmart-cards)
