# 🛠 Установка ПАРТМАРТ Cards Generator

## Требования

- Python 3.10+
- pip
- Virtualenv (рекомендуется)

## Быстрая установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/vitorpixel-6436/partmart-cards.git
cd partmart-cards
```

### 2. Создание виртуального окружения

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Миграции базы данных

```bash
python manage.py migrate
```

### 5. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### 6. Сбор статических файлов (для production)

```bash
python manage.py collectstatic --noinput
```

### 7. Запуск сервера

```bash
python manage.py runserver
```

Откройте браузер: http://127.0.0.1:8000

## Настройка шрифтов

Для корректной генерации карточек убедитесь, что установлены необходимые шрифты:

**Linux:**
```bash
sudo apt-get install fonts-dejavu fonts-liberation
```

**Mac:**
Шрифты уже установлены в системе

**Windows:**
Шрифты Arial установлены по умолчанию

## Структура директорий

После запуска автоматически создадутся:

```
media/
├── uploads/          # Загруженные фото ПК
└── generated/        # Сгенерированные карточки
```

## Troubleshooting

### Ошибка импорта PIL

```bash
pip install --upgrade Pillow
```

### Ошибка миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Проблемы с правами на media/

```bash
chmod -R 755 media/
```

## Production настройка

1. Измените `SECRET_KEY` в `settings.py`
2. Установите `DEBUG = False`
3. Настройте `ALLOWED_HOSTS`
4. Используйте nginx + gunicorn
5. Настройте HTTPS

## Готово! 🎉

Теперь вы можете создавать профессиональные карточки ПАРТМАРТ!
